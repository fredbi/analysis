schemes:
  - http
swagger: "2.0"
info:
  title: test
  version: "1.0"
paths:
  /a:
    get:
      operationId: a
      responses:
        "200":
          description: OK
          schema:
            type: array
            items:
              $ref: ../definitions/a.yaml
    post:
      operationId: b
      responses:
        "200":
          description: c
          schema:
            $ref: ../definitions/a.yaml
